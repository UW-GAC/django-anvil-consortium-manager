version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-20.04
  tools:
    python: "3.9"
  jobs:
    build:
      html:
        - pip install hatch
        - export SPHINXBUILDDIR=../$READTHEDOCS_OUTPUT
        - hatch run docs:build
